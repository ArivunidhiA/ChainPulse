# Neon Postgres connection string (required for Vercel serverless API routes).
# Set in Vercel project settings → Environment Variables.
# Use DATABASE_URL or POSTGRES_URL (Vercel Postgres uses POSTGRES_URL).
DATABASE_URL=
