# Alchemy Ethereum RPC (set your key)
ALCHEMY_API_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Neon Postgres — set DATABASE_URL to your Neon connection string (with sslmode=require)
DATABASE_URL=postgresql://user:password@your-neon-host/neondb?sslmode=require

# Optional: discrete vars for dbt profile (Neon)
PGHOST=your-neon-host
PGPORT=5432
PGDATABASE=neondb
PGUSER=neondb_owner
PGPASSWORD=your-password
PGSSLMODE=require

# CoinGecko (optional API key for higher rate limits)
COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
COINGECKO_API_KEY=

# Backend
BACKEND_PORT=8000
BACKEND_LOG_LEVEL=info

# CORS
FRONTEND_ORIGIN=http://localhost:3000

# dbt
DBT_PROFILES_DIR=/app/dbt_chainpulse
